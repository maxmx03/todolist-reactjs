import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    bg: '#191919',
    bgdark: '#141414',
    bgdarker: '#0f0f0f',
    bglight: '#333333',
    grey: '#D0D4CA'
  },
})

export default theme
